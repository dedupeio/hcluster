branches:
  only:
  - master
  - /^v.*$/
language: python
notifications:
  email:
    on_success: change
    on_failure: always
  irc: chat.freenode.net#dedupe
matrix:
  include:
  - os: linux
    python: 3.4
  - os: linux
    python: 2.7
  - os: osx
    language: generic
    env: TERRYFY_PYTHON='system 2'
  - os: osx
    language: generic
    env: TERRYFY_PYTHON='homebrew 2'
  - os: osx
    language: generic
    env: TERRYFY_PYTHON='homebrew 3'
before_install:
- if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then git clone https://github.com/MacPython/terryfy;
  fi
- if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then source terryfy/travis_tools.sh; fi
- if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then get_python_environment $TERRYFY_PYTHON
  venv; fi
- if [[ "$TERRYFY_PYTHON" == "homebrew 3" ]]; then alias pip=`which pip3` ; fi
install:
- pip install numpy
- pip install -r requirements.txt
- cython hcluster/_hierarchy.pyx
- pip install -e .
script:
- nosetests
after_success:
- coveralls
- echo [distutils]                                  > ~/.pypirc
- echo index-servers = pypi                        >> ~/.pypirc
- echo [pypi]                                      >> ~/.pypirc
- echo repository=https://pypi.python.org/pypi     >> ~/.pypirc
- echo username=fgregg                             >> ~/.pypirc
- echo password=$PYPIPASSWORD                      >> ~/.pypirc
- if [[ $TRAVIS_TAG && "$TRAVIS_OS_NAME" == "osx" ]]; then python setup.py bdist_wheel upload; fi
- if [[ $TRAVIS_TAG && "$TRAVIS_OS_NAME" == "linux" ]]; then python setup.py sdist upload; fi
env:
  global:
  - secure: Ek4VsurYtjsntbnFVpwJy4or7lR6jJuu+yLEvYf9ZAfAann29eNF0b8WH+/Aw6zVfdz6Z8aAZSFwzomjmMOET+MjXjIpCe8yyyRDB7BJZXcGBTqFwiIe90Pbh9469VOhnQw5b9pDxOPPWI27NJHVfKpo6bsbbgJuhXzPAWCfuvQ=

